type ProductType = {
  id: number;
  room_name: string;
  images: string;
  price: number;
  address: string;
  category: string;
  guest: number;
  schedule: string;
  // schedule_date={product.schedule_date}
  // handleClick={handleClick}
};
